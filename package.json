{
  "name": "bottomless-box",
  "version": "0.0.1",
  "type": "module",
  "description": "scrapes game distributors to compare prices for games on your Steam wishlist.",
  "dependencies": {
    "crawlee": "^3.0.0",
    "playwright": "*",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@apify/tsconfig": "^0.1.0",
    "@types/node": "^20.0.0",
    "cpx": "^1.5.0",
    "tsx": "^4.4.0",
    "typescript": "~5.3.0"
  },
  "scripts": {
    "crawler": "npm run start:dev",
    "start:prod": "node dist/main.js",
    "start:dev": "tsx src/main.ts",
    "dev": "ts-node --esm src/server.ts",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cpx \"src/client/**/*\" dist/src/client",
    "start": "node dist/src/server.js"
  },
  "author": "rkraeher",
  "license": "ISC"
}
